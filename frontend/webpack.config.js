export const performance = {
    maxAssetSize: 1000000, // Adjust max size (in bytes) for the asset size warning
    maxEntrypointSize: 1000000, // Adjust max size for the entry point size warning
};
  